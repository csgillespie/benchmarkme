test_that("Test benchmark_std", {
  skip_on_cran()
  skip_on_travis()
  expect_error(benchmark_std(runs = 0))
  res = benchmark_std(runs = 1)
  res2 = benchmark_std(runs = 1, cores=2)
  # res3 = benchmark_std(runs = 5)
  # res4 = benchmark_std(runs = 5, cores = 2)
  expect_equal(nrow(res), 15)
  expect_equal(nrow(res2), 30)
  expect_equal(ncol(res), 6)
  expect_equal(ncol(res2), 6)
})